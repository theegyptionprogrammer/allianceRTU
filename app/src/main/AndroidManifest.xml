<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.alliancertu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".items.SessionItem"
            android:label="@string/title_activity_session_item"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".sportClubUI.sportEvents.information.FragmentSportEventInformations" />
        <activity android:name=".models.Player" />
        <activity android:name=".ui.national_teams.NationalTeamActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sportClubUI.sportEvents.SportEventActivity" />
        <activity android:name=".sportClubUI.session.SessionActivity" />
        <activity android:name=".nationalTeamsUI.schedule_matches.DetailItemRvScheduleMatchesActivity" />
    </application>

</manifest>